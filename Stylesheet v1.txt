<style>




  @import url('https://fonts.googleapis.com/css2?family=Sora:wght@600;700;800&family=Inter:wght@400;500;600;700&family=Orbitron:wght@600;700;800&family=JetBrains+Mono:wght@500;600&display=swap');

  :root {
    --radius-sm: 8px;
    --radius-md: 12px;
    --radius-lg: 20px;
    --radius: 12px;
    --transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* ============ 1. LIGHT THEME (Gundam Corporate - White & Royal Cobalt Navy) ============ */
  body.theme-light {
    --bg: #edf2f9;
    --bg-gradient: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 40%, #dbeafe 100%);
    --surface: #ffffff;
    --surface-alt: #f1f5f9;
    --border: #cbd5e1;
    --text: #0f172a;
    --text-muted: #475569;
    --primary: #1d4ed8;
    --primary-hover: #1e40af;
    --primary-soft: #dbeafe;
    --accent: #0284c7;
    --accent-glow: rgba(2, 132, 199, 0.2);
    --success: #16a34a;
    --danger: #dc2626;
    --shadow: 0 4px 20px -2px rgba(15, 23, 42, 0.08), 0 2px 6px -1px rgba(15, 23, 42, 0.04);
    --sidebar-bg: #0f172a;
    --sidebar-text: #f1f5f9;
    --sidebar-active: #2563eb;
    --input-bg: #ffffff;
    --font-body: 'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif;
    --font-display: 'Sora', 'Inter', system-ui, sans-serif;
  }

  /* ============ 2. DARK THEME (Cyberpunk - Gundam Mecha vs Godzilla Atomic Kaiju) ============ */
  body.theme-dark {
    --bg: #090d16;
    --bg-gradient: radial-gradient(circle at 20% 20%, #111827 0%, #090d16 65%, #05070e 100%);
    --surface: #111827;
    --surface-alt: #1e293b;
    --border: #334155;
    --text: #f8fafc;
    --text-muted: #94a3b8;
    --primary: #00f0ff; /* Gundam Beam / Neon Cyan */
    --primary-hover: #00c8d6;
    --primary-soft: rgba(0, 240, 255, 0.15);
    --accent: #ff007f;  /* Godzilla Atomic Pulse / Neon Magenta */
    --accent-glow: rgba(255, 0, 127, 0.35);
    --success: #10b981; /* Kaiju Emerald Radiation */
    --danger: #ff3355;
    --shadow: 0 8px 32px rgba(0, 240, 255, 0.12), 0 0 0 1px rgba(255, 0, 127, 0.15);
    --sidebar-bg: #0b0f19;
    --sidebar-text: #cbd5e1;
    --sidebar-active: #00f0ff;
    --input-bg: #1e293b;
    --font-body: 'Inter', 'Segoe UI', system-ui, sans-serif;
    --font-display: 'Orbitron', 'Sora', system-ui, sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
  }

  /* ---------- Global Grid Overlay Effects ---------- */
  body.theme-light::before {
    content: '';
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 9999;
    background-image: 
      linear-gradient(rgba(37, 99, 235, 0.025) 1px, transparent 1px),
      linear-gradient(90deg, rgba(37, 99, 235, 0.025) 1px, transparent 1px);
    background-size: 36px 36px, 36px 36px;
    opacity: 0.8;
  }

  body.theme-dark::before {
    content: '';
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 9999;
    background-image: 
      linear-gradient(rgba(0, 240, 255, 0.035) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 0, 127, 0.035) 1px, transparent 1px);
    background-size: 40px 40px, 40px 40px;
    mix-blend-mode: screen;
    opacity: 0.7;
  }

  /* ============ 3. BASE RESET & MAPPING ============ */
  * { box-sizing: border-box; }
  html, body {
    margin: 0; padding: 0;
    background: var(--bg-gradient);
    background-attachment: fixed;
    color: var(--text);
    font-family: var(--font-body);
    transition: background var(--transition), color var(--transition);
    min-height: 100vh;
  }

  h1, h2, h3, h4, .brand-text, .nav-item.active, .sidebar-brand span, .login-card h1 {
    font-family: var(--font-display);
    letter-spacing: -0.02em;
  }

  body.theme-dark h1, body.theme-dark h2, body.theme-dark h3 {
    text-shadow: 0 0 10px var(--primary-soft), 0 0 2px var(--primary);
    color: var(--primary);
  }

  a { color: var(--primary); }
  button { font-family: inherit; cursor: pointer; }



  /* ---------- Theme toggle ---------- */
  .theme-toggle {
    position: fixed; top: 16px; right: 16px; z-index: 500;
    display: flex; align-items: center; gap: 8px;
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 999px; padding: 6px 14px; box-shadow: var(--shadow);
    font-size: 13px; color: var(--text-muted);
  }
  .theme-toggle button {
    border: none; background: var(--primary); color: #fff;
    border-radius: 999px; width: 34px; height: 24px; position: relative;
  }
  body.theme-dark .theme-toggle button { box-shadow: 0 0 10px var(--primary); }

  /* ---------- Login screen ---------- */
  #loginScreen {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    background: var(--bg);
  }
  .login-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 28px;
    width: 100%;
    box-sizing: border-box;
  }
  .login-topbar .brand-title {
    font-family: 'Sora', sans-serif;
    font-weight: 800;
    font-size: 18px;
    color: #0038b8;
    letter-spacing: -0.3px;
  }
  body.theme-dark .login-topbar .brand-title {
    color: var(--primary);
  }
  .login-topbar-actions {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .topbar-btn {
    background: transparent;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    padding: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    transition: background 0.2s, color 0.2s;
  }
  .topbar-btn:hover {
    background: var(--surface-alt);
    color: var(--text);
  }
  .login-center-body {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px 16px 40px;
  }
  .login-card {
    position: relative;
    width: 100%;
    max-width: 440px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    box-shadow: 0 12px 40px rgba(15, 23, 42, 0.08);
    padding: 36px 32px;
    text-align: center;
    box-sizing: border-box;
  }
  body.theme-dark .login-card {
    backdrop-filter: blur(12px);
    background: rgba(17, 24, 39, 0.85);
    border-color: rgba(0, 240, 255, 0.25);
    box-shadow: 0 0 30px rgba(0, 240, 255, 0.15);
  }

  .mecha-corner {
    position: absolute;
    width: 12px;
    height: 12px;
    pointer-events: none;
  }
  .mecha-corner-tl {
    top: -2px;
    left: -2px;
    border-top: 3px solid #0038b8;
    border-left: 3px solid #0038b8;
  }
  .mecha-corner-br {
    bottom: -2px;
    right: -2px;
    border-bottom: 3px solid #0038b8;
    border-right: 3px solid #0038b8;
  }
  body.theme-dark .mecha-corner-tl, body.theme-dark .mecha-corner-br {
    border-color: var(--primary);
  }

  .mecha-border {
    position: relative;
    border: 1px solid var(--border);
    background: var(--surface);
    border-radius: 6px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .mecha-border::before, .mecha-border::after {
    content: '';
    position: absolute;
    width: 8px;
    height: 8px;
    border-color: #0037b0;
    pointer-events: none;
  }
  .mecha-border::before {
    top: -1px;
    left: -1px;
    border-top: 2px solid #0037b0;
    border-left: 2px solid #0037b0;
  }
  .mecha-border::after {
    bottom: -1px;
    right: -1px;
    border-bottom: 2px solid #0037b0;
    border-right: 2px solid #0037b0;
  }
  body.theme-dark .mecha-border::before, body.theme-dark .mecha-border::after {
    border-color: var(--primary);
  }
  body.theme-dark .mecha-border {
    border-color: rgba(0, 240, 255, 0.25);
    background: rgba(17, 24, 39, 0.85);
  }

  .label-tech {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .mecha-card-badge {
    position: absolute;
    top: 0;
    left: 0;
    background: #0037b0;
    color: #ffffff;
    padding: 3px 10px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.05em;
    border-bottom-right-radius: 6px;
    z-index: 2;
  }
  body.theme-dark .mecha-card-badge {
    background: var(--primary);
    color: #000000;
  }

  .mecha-helper-banner {
    background: rgba(0, 55, 176, 0.06);
    border-left: 4px solid #0037b0;
    padding: 14px 18px;
    border-radius: 0 6px 6px 0;
    margin-bottom: 20px;
    display: flex;
    align-items: flex-start;
    gap: 12px;
  }
  body.theme-dark .mecha-helper-banner {
    background: rgba(0, 240, 255, 0.08);
    border-left-color: var(--primary);
  }

  .badge-auto-fill {
    font-family: 'JetBrains Mono', monospace;
    font-size: 10px;
    color: #10b981;
    background: rgba(16, 185, 129, 0.1);
    padding: 2px 6px;
    border-radius: 4px;
    margin-left: 6px;
    font-weight: 500;
  }

  .login-logos {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
    margin-bottom: 24px;
    padding: 10px 14px;
    border-radius: 8px;
    background: var(--surface-alt);
    border: 1px solid var(--border);
  }
  .login-logo { height: 48px; width: auto; object-fit: contain; }
  .login-logo--undip { height: 54px; }
  .login-logo--dsdm { height: 44px; }
  body.theme-light .login-logo--dsdm {
    filter: brightness(0.18) contrast(1.2);
  }
  body.theme-dark .login-logo--dsdm {
    filter: none;
  }
  .logo-divider {
    width: 1px;
    height: 36px;
    background: var(--border);
  }

  .login-card h1 {
    font-family: 'Sora', sans-serif;
    font-size: 21px;
    font-weight: 700;
    color: var(--text);
    margin: 0 0 6px 0;
    letter-spacing: -0.3px;
    line-height: 1.3;
  }
  .login-card p.subtitle {
    color: var(--text-muted);
    font-size: 13.5px;
    margin: 0 0 24px 0;
  }
  body.theme-dark .login-card h1 {
    color: var(--primary);
    text-shadow: 0 0 8px rgba(0, 240, 255, 0.4);
  }

  /* ---------- Auth switch ---------- */
  .auth-switch {
    display: flex;
    gap: 4px;
    margin-bottom: 24px;
    padding: 4px;
    background: var(--surface-alt);
    border: 1px solid var(--border);
    border-radius: 8px;
  }
  .auth-switch-btn {
    flex: 1;
    border: none;
    background: transparent;
    color: var(--text-muted);
    padding: 10px 12px;
    border-radius: 6px;
    font-size: 12.5px;
    font-weight: 700;
    letter-spacing: 0.5px;
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .auth-switch-btn.active {
    background: #0038b8;
    color: #ffffff;
    box-shadow: 0 2px 8px rgba(0, 56, 184, 0.25);
  }
  body.theme-dark .auth-switch-btn.active {
    background: var(--primary);
    color: #090d16;
    box-shadow: 0 0 12px var(--primary);
  }
  .auth-pane { display: none; }
  .auth-pane.active { display: block; }

  /* Inputs & Fields */
  .field { text-align: left; margin-bottom: 16px; }
  .field label {
    display: block; font-size: 12.5px; font-weight: 600;
    color: var(--text-muted); margin-bottom: 6px; text-transform: uppercase; letter-spacing: 0.4px;
  }
  input, select, textarea {
    width: 100%;
    box-sizing: border-box;
    background: var(--surface-alt);
    border: 1px solid var(--border);
    color: var(--text);
    border-radius: var(--radius-sm);
    padding: 10px 14px;
    font-family: inherit;
    transition: border var(--transition), box-shadow var(--transition);
  }

  .input-with-icon {
    position: relative;
    width: 100%;
    box-sizing: border-box;
  }
  .input-with-icon .input-icon-left {
    position: absolute;
    left: 12px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--text-muted);
    pointer-events: none;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
  }
  .input-with-icon input {
    width: 100% !important;
    box-sizing: border-box !important;
    padding-left: 40px !important;
    height: 44px;
    font-size: 14px;
  }
  .input-with-icon input.has-icon-right {
    padding-right: 42px !important;
  }
  .input-with-icon .input-icon-right-btn {
    position: absolute;
    right: 8px;
    top: 50%;
    transform: translateY(-50%);
    background: transparent;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    padding: 6px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 4px;
    z-index: 2;
  }
  .input-with-icon .input-icon-right-btn:hover {
    color: var(--text);
  }

  .info-banner {
    margin-top: 8px;
    display: flex;
    align-items: flex-start;
    gap: 8px;
    background: var(--primary-soft);
    border: 1px solid rgba(29, 78, 216, 0.2);
    padding: 9px 12px;
    border-radius: 6px;
    color: var(--primary);
    font-size: 12.5px;
    line-height: 1.4;
  }
  body.theme-dark .info-banner {
    border-color: rgba(0, 240, 255, 0.3);
    color: var(--primary);
  }
  body.theme-dark input, body.theme-dark select, body.theme-dark textarea {
    font-family: var(--font-mono, 'JetBrains Mono', monospace);
    border-color: var(--primary-soft);
  }
  input:focus, select:focus, textarea:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 3px var(--primary-soft);
  }
  body.theme-dark input:focus, body.theme-dark select:focus, body.theme-dark textarea:focus {
    box-shadow: 0 0 0 3px var(--primary-soft), 0 0 12px var(--primary);
  }
  body.theme-dark input::placeholder { color: #6a5f96; }

  /* Buttons */
  .btn {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    padding: 11px 18px; border-radius: var(--radius-sm); border: none;
    font-size: 14px; font-weight: 600; transition: all var(--transition);
  }
  .btn-primary {
    background: var(--primary);
    color: #fff;
    border: none;
    border-radius: var(--radius-sm);
    padding: 10px 20px;
    font-weight: 600;
    width: 100%;
    transition: all var(--transition);
  }
  .btn-primary:hover {
    background: var(--primary-hover);
    transform: translateY(-1px);
  }
  body.theme-dark .btn-primary {
    box-shadow: 0 0 15px var(--primary-soft);
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .btn-secondary {
    background: var(--surface-alt); color: var(--text); border: 1px solid var(--border);
  }
  .btn-outline {
    background: transparent; color: var(--primary); border: 1px solid var(--primary);
  }
  .btn-danger { background: var(--danger); color: #fff; }
  .btn:disabled { opacity: 0.6; cursor: not-allowed; }

  .error-box {
    background: rgba(214,69,69,0.1); border: 1px solid var(--danger);
    color: var(--danger); font-size: 13px; padding: 9px 12px;
    border-radius: var(--radius-sm); margin-bottom: 14px; text-align: left; display: none;
  }

  /* ---------- App shell ---------- */
  #appShell { display: none; min-height: 100vh; }
  .app-layout { display: flex; min-height: 100vh; }

  /* Sidebar Custom Styling */
  .sidebar {
    width: 240px; flex-shrink: 0;
    background: var(--sidebar-bg);
    border-right: 1px solid var(--border);
    display: flex; flex-direction: column;
    position: relative;
    color: var(--sidebar-text); padding: 22px 14px;
  }

  .sidebar-brand {
    display: flex; align-items: center; gap: 10px; margin-bottom: 26px; padding: 0 6px 16px 6px;
    border-bottom: 1px solid rgba(255,255,255,0.1);
  }
  .sidebar-brand-logo {
    height: 38px;
    width: auto;
    object-fit: contain;
    flex-shrink: 0;
    filter: drop-shadow(0 2px 6px rgba(0,0,0,0.3));
  }
  .sidebar-brand span { font-weight: 700; font-size: 13px; line-height: 1.25; color: #fff; letter-spacing: 0.3px; }
  body.theme-dark .sidebar-brand span {
    color: var(--primary); text-transform: uppercase; font-size: 12px; letter-spacing: 0.8px;
    text-shadow: 0 0 6px var(--primary), 0 0 14px rgba(0,229,255,0.55);
  }

  .nav-item {
    padding: 12px 16px;
    display: flex; align-items: center; gap: 12px;
    color: var(--sidebar-text); cursor: pointer;
    transition: all 0.2s ease;
    font-size: 14px; font-weight: 500;
    border-radius: var(--radius-sm); margin-bottom: 4px;
  }

  .nav-item:hover {
    background: rgba(255,255,255,0.08);
    color: white;
  }

  .nav-item.active {
    background: var(--primary);
    color: white;
    font-weight: 700;
    border-left: 4px solid var(--accent);
    box-shadow: 0 0 14px var(--primary-soft);
  }

  .sidebar-footer { margin-top: auto; }
  .user-chip {
    background: rgba(255,255,255,0.06); border-radius: var(--radius-sm); padding: 10px 12px;
    font-size: 12.5px; margin-bottom: 8px;
  }
  .user-chip .u-name { font-weight: 700; color: #fff; display: block; margin-bottom: 2px; }

  .main-content { flex: 1; padding: 26px 30px; min-width: 0; }
  .topbar {
    display: flex; align-items: center; justify-content: space-between; margin-bottom: 22px; padding-right: 120px;
  }
  .topbar h2 { margin: 0; font-size: 21px; }
  .topbar .breadcrumb { font-size: 12.5px; color: var(--text-muted); }
  body.theme-dark .topbar h2 {
    color: var(--accent); letter-spacing: 0.4px;
    text-shadow: 0 0 6px var(--accent), 0 0 16px rgba(255,46,224,0.45);
  }

  /* Mecha Card Style */
  .card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    box-shadow: var(--shadow);
    padding: 24px;
    margin-bottom: 20px;
    overflow: hidden;
    position: relative;
  }

  body.theme-dark .card {
    background: rgba(17, 24, 39, 0.8);
    backdrop-filter: blur(10px);
    border-color: rgba(0, 240, 255, 0.2);
  }

  .card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; width: 100%; height: 2px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    opacity: 0.7;
  }

  /* Multi-select Chips & Mecha Badges */
  .mecha-chip, .struktural-chip {
    padding: 8px 16px;
    border-radius: 999px;
    border: 1px solid var(--border);
    background: var(--surface-alt);
    cursor: pointer;
    font-weight: 600;
    font-size: 13px;
    transition: all 0.2s ease;
  }

  .mecha-chip.active, .struktural-chip:has(input:checked) {
    background: var(--primary);
    color: white;
    border-color: var(--primary);
    box-shadow: 0 0 10px var(--primary-soft);
  }

  .badge-autofill {
    background: #dcfce7;
    color: #15803d;
    font-size: 10px;
    font-weight: 700;
    padding: 2px 6px;
    border-radius: 4px;
    text-transform: uppercase;
  }

  body.theme-dark .badge-autofill {
    background: rgba(16, 185, 129, 0.2);
    color: #10b981;
    border: 1px solid rgba(16, 185, 129, 0.4);
  }

  .tabs {
    display: flex;
    gap: 4px;
    margin-bottom: 20px;
    border-bottom: 1px solid var(--border);
    overflow-x: auto;
    padding-bottom: 2px;
  }
  .tab-btn {
    background: transparent;
    border: none;
    padding: 12px 18px;
    font-family: 'Sora', sans-serif;
    font-size: 13.5px;
    font-weight: 600;
    color: var(--text-muted);
    border-bottom: 3px solid transparent;
    margin-bottom: -1px;
    white-space: nowrap;
    cursor: pointer;
    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
    border-radius: 6px 6px 0 0;
  }
  .tab-btn:hover {
    color: var(--primary);
    background: var(--surface-alt);
  }
  .tab-btn.active {
    color: var(--primary);
    background: var(--surface-alt);
    border-bottom-color: var(--primary);
    font-weight: 700;
  }
  body.theme-dark .tab-btn.active {
    color: var(--primary);
    border-bottom-color: var(--primary);
    box-shadow: inset 0 -2px 0 var(--primary);
  }
  .tab-panel { display: none; }
  .tab-panel.active { display: block; }

  .autocomplete-wrap { position: relative; }
  .autocomplete-list {
    position: absolute; top: 100%; left: 0; right: 0; z-index: 50;
    background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-sm);
    box-shadow: var(--shadow); max-height: 220px; overflow-y: auto; display: none; margin-top: 4px;
  }
  .autocomplete-item { padding: 9px 13px; font-size: 13.5px; cursor: pointer; }
  .autocomplete-item:hover { background: var(--primary-soft); }
  .autocomplete-item small { display: block; color: var(--text-muted); }

  .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  .grid-full { grid-column: 1 / -1; }
  @media (max-width: 720px) { .grid-2 { grid-template-columns: 1fr; } }

  .stack-field { margin-bottom: 14px; }

  /* Tables */
  table {
    width: 100%;
    border-collapse: collapse;
  }
  th {
    background: var(--surface-alt);
    color: var(--text-muted);
    text-align: left;
    padding: 12px;
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  td {
    padding: 16px 12px;
    border-bottom: 1px solid var(--border);
  }

  table.dyn-table { width: 100%; border-collapse: collapse; font-size: 13px; }
  table.dyn-table th, table.dyn-table td {
    border: 1px solid var(--border); padding: 8px; text-align: left;
  }
  table.dyn-table th { background: var(--surface-alt); font-size: 12px; text-transform: uppercase; }
  table.dyn-table select, table.dyn-table input { padding: 6px 8px; font-size: 13px; }
  .row-remove-btn { background: none; border: none; color: var(--danger); font-size: 16px; }

  /* White text style ONLY for Jenis Usulan & Evaluasi Kinerja dropdown fields */
  #kontrakUser_jenisUsulan,
  #kontrakUser_evaluasiKinerja,
  #kontrakUser_jenisUsulan option,
  #kontrakUser_evaluasiKinerja option {
    color: #ffffff !important;
    -webkit-text-fill-color: #ffffff !important;
    font-weight: 700 !important;
  }

  /* Locked / Read-Only input text color: BLACK (#000000) */
  #kontrakUserForm input:read-only,
  #kontrakUserForm input:disabled,
  #kontrakUserForm input[readonly],
  #kontrakUserForm select:disabled,
  #kontrakUserForm select[readonly] {
    color: #000000 !important;
    -webkit-text-fill-color: #000000 !important;
    font-weight: 700 !important;
  }

  .actions-bar { display: flex; gap: 10px; justify-content: flex-end; margin-top: 18px; }

  .modal-overlay {
    position: fixed; inset: 0; background: rgba(0,0,0,0.55); z-index: 900;
    display: none; align-items: center; justify-content: center; padding: 24px;
  }
  .modal-box {
    background: var(--surface); border-radius: var(--radius-md); width: 100%; max-width: 860px;
    max-height: 88vh; display: flex; flex-direction: column; box-shadow: var(--shadow);
    border: 1px solid var(--border); overflow: hidden;
  }
  .modal-header {
    display: flex; align-items: center; justify-content: space-between;
    padding: 14px 18px; border-bottom: 1px solid var(--border);
  }
  .modal-body { flex: 1; overflow: auto; padding: 0; }
  .modal-body iframe { width: 100%; height: 70vh; border: none; }

  .toast-container {
    position: fixed; bottom: 20px; right: 20px; z-index: 1000;
    display: flex; flex-direction: column; gap: 10px;
  }
  .toast {
    background: var(--surface); border: 1px solid var(--border); border-left: 4px solid var(--primary);
    box-shadow: var(--shadow); border-radius: var(--radius-sm); padding: 12px 16px; font-size: 13.5px;
    min-width: 240px; animation: toastIn 0.25s ease;
  }
  .toast.error { border-left-color: var(--danger); }
  .toast.success { border-left-color: var(--success); }
  @keyframes toastIn { from { opacity: 0; transform: translateY(8px);} to {opacity:1; transform:none;} }

  .table-list { width: 100%; border-collapse: collapse; font-size: 13.5px; }
  .table-list th, .table-list td { padding: 10px; border-bottom: 1px solid var(--border); text-align: left; }
  .table-list th { color: var(--text-muted); font-size: 11.5px; text-transform: uppercase; }

  .spinner {
    width: 16px; height: 16px; border-radius: 50%;
    border: 2px solid rgba(255,255,255,0.4); border-top-color: #fff;
    animation: spin 0.7s linear infinite; display: inline-block;
  }
  @keyframes spin { to { transform: rotate(360deg); } }

  .helper-text { font-size: 12px; color: var(--text-muted); margin-top: 4px; }

  /* ---------- Glosarium Tag (Menu Lainnya) ---------- */
  body.theme-light {
    --glossary-accent-1: var(--primary);
    --glossary-accent-2: var(--accent);
    --glossary-accent-3: #7c5cbf;
  }
  body.theme-dark {
    --glossary-accent-1: var(--primary);
    --glossary-accent-2: var(--accent);
    --glossary-accent-3: #a970ff;
  }

  .glossary-section-title {
    color: var(--text); margin: 24px 0 10px; font-size: 14px;
    letter-spacing: 0.02em; font-weight: 700;
  }
  .glossary-grid {
    display: grid; grid-template-columns: repeat(auto-fill, minmax(230px, 1fr));
    gap: 14px; margin-bottom: 6px;
  }
  .glossary-card {
    background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-md);
    padding: 16px; box-shadow: var(--shadow); transition: transform 0.2s ease, box-shadow var(--transition);
  }
  .glossary-card:hover { transform: translateY(-2px); }
  .glossary-card.accent-1 { border-color: var(--glossary-accent-1); }
  .glossary-card.accent-2 { border-color: var(--glossary-accent-2); }
  .glossary-card.accent-3 { border-color: var(--glossary-accent-3); }
  body.theme-dark .glossary-card.accent-1 { box-shadow: 0 0 14px rgba(0,229,255,0.25); }
  body.theme-dark .glossary-card.accent-2 { box-shadow: 0 0 14px rgba(255,46,224,0.25); }
  body.theme-dark .glossary-card.accent-3 { box-shadow: 0 0 14px rgba(169,112,255,0.25); }

  .glossary-card .card-icon {
    width: 34px; height: 34px; border-radius: 9px; display: flex; align-items: center;
    justify-content: center; background: var(--primary-soft); margin-bottom: 10px; font-size: 15px;
  }
  .glossary-card .card-label {
    font-size: 11px; text-transform: uppercase; letter-spacing: 0.03em;
    color: var(--text-muted); margin-bottom: 4px;
  }
  .glossary-card code {
    font-family: 'JetBrains Mono', monospace; font-size: 13px; color: var(--text);
    word-break: break-word; display: block; margin-bottom: 6px;
  }
  .glossary-card .btn-copy {
    margin-top: 6px; padding: 5px 12px; font-size: 12px; border-radius: var(--radius-sm);
    border: 1px solid var(--border); background: transparent; color: var(--primary);
  }
  .glossary-card .btn-copy:hover { background: var(--primary-soft); }

  /* ---------- Baca Rumus dari Template: hasil scan ---------- */
  .tag-chip {
    display: inline-flex; padding: 4px 10px; border-radius: 999px;
    background: var(--primary-soft); border: 1px solid var(--border);
  }
  .tag-chip code { font-family: 'JetBrains Mono', monospace; font-size: 12px; color: var(--text); }
  .tag-chip.accent-1 { border-color: var(--glossary-accent-1); }
  .tag-chip.accent-3 { border-color: var(--glossary-accent-3); }
  .tag-chip.accent-warning { border-color: #e2a13a; background: rgba(226,161,58,0.12); }

  .glossary-card.accent-warning { border-color: #e2a13a; }
  body.theme-dark .glossary-card.accent-warning { box-shadow: 0 0 14px rgba(226,161,58,0.25); }

  .input-text {
    padding: 11px 13px; border-radius: var(--radius-sm); border: 1px solid var(--border);
    background: var(--input-bg); color: var(--text); font-size: 14px;
  }
  .input-text:focus {
    outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px var(--primary-soft);
  }

  /* =========================================================================
     KENAIKAN PANGKAT — Check Eligible / Opsi A / Opsi B
     ========================================================================= */
  body.theme-light .kp-panel {
    --kp-field-bg: #123467;
    --kp-field-bg-hover: #163f7d;
    --kp-field-text: #eef4ff;
    --kp-field-placeholder: rgba(230, 238, 255, 0.62);
    --kp-field-border: #0d2a56;
    --kp-label: #3a5d94;
    --kp-card-bg: #ffffff;
    --kp-card-border: #dbe6f7;
  }
  body.theme-dark .kp-panel {
    --kp-field-bg: rgba(20, 14, 36, 0.85);
    --kp-field-bg-hover: rgba(28, 21, 48, 0.95);
    --kp-field-text: #eafcff;
    --kp-field-placeholder: #6a5f96;
    --kp-field-border: rgba(0, 229, 255, 0.35);
    --kp-label: #ff8ce8;
    --kp-card-bg: rgba(18, 13, 32, 0.55);
    --kp-card-border: rgba(0, 229, 255, 0.22);
  }

  .kp-panel .kp-label,
  .kp-panel .stack-field > label {
    display: block; font-size: 11.5px; font-weight: 700;
    color: var(--kp-label); margin-bottom: 7px;
    text-transform: uppercase; letter-spacing: 0.55px;
  }
  body.theme-dark .kp-panel .stack-field > label {
    font-family: var(--font-display); text-shadow: 0 0 6px rgba(255, 140, 232, 0.35);
  }

  .kp-panel .stack-field input,
  .kp-panel .stack-field select,
  .kp-panel .field-input {
    width: 100%; padding: 13px 16px; border-radius: 11px;
    border: 1px solid var(--kp-field-border);
    background: var(--kp-field-bg); color: var(--kp-field-text);
    font-size: 14px; font-weight: 500;
    transition: border var(--transition), box-shadow var(--transition), background var(--transition);
  }
  .kp-panel .stack-field input::placeholder,
  .kp-panel .field-input::placeholder { color: var(--kp-field-placeholder); }
  .kp-panel .stack-field input:hover,
  .kp-panel .stack-field select:hover,
  .kp-panel .field-input:hover { background: var(--kp-field-bg-hover); }
  .kp-panel .stack-field input:focus,
  .kp-panel .stack-field select:focus,
  .kp-panel .field-input:focus {
    outline: none; background: var(--kp-field-bg-hover);
    border-color: var(--primary); box-shadow: 0 0 0 3px var(--primary-soft);
  }
  body.theme-dark .kp-panel .stack-field input,
  body.theme-dark .kp-panel .stack-field select,
  body.theme-dark .kp-panel .field-input { font-family: 'JetBrains Mono', monospace; }
  body.theme-dark .kp-panel .stack-field input:focus,
  body.theme-dark .kp-panel .stack-field select:focus,
  body.theme-dark .kp-panel .field-input:focus {
    box-shadow: 0 0 0 3px var(--primary-soft), 0 0 14px var(--primary);
    border-color: var(--primary);
  }
  .kp-panel .stack-field select,
  .kp-panel select.field-input { color-scheme: dark; }
  body.theme-light .kp-panel .stack-field select,
  body.theme-light .kp-panel select.field-input { color-scheme: dark; }

  .kp-panel input[readonly] { opacity: 0.75; cursor: not-allowed; }

  .kp-panel .grid-2.kp-grid { gap: 18px 22px; margin-top: 4px; }
  .kp-panel .stack-field { margin-bottom: 16px; }

  /* ---------- Toolbar "+ Tambah Data" ---------- */
  .kp-entries-toolbar {
    display: flex; align-items: center; justify-content: space-between; gap: 14px;
    flex-wrap: wrap; margin-bottom: 16px; padding: 10px 14px;
    background: var(--surface-alt); border: 1px dashed var(--border); border-radius: 10px;
  }
  body.theme-dark .kp-entries-toolbar { border-color: rgba(0,229,255,0.3); }

  /* ---------- Kartu entri (satu = satu bagian dokumen) ---------- */
  .kp-entry-card {
    background: var(--kp-card-bg); border: 1px solid var(--kp-card-border);
    border-radius: 14px; padding: 20px 20px 6px; margin-bottom: 18px;
    box-shadow: var(--shadow); position: relative;
  }
  body.theme-dark .kp-entry-card { box-shadow: 0 0 0 1px rgba(0,229,255,0.08), 0 8px 24px rgba(255,46,224,0.10); }

  .kp-entry-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 16px; padding-bottom: 12px; border-bottom: 1px solid var(--border);
  }
  body.theme-dark .kp-entry-header { border-bottom-color: rgba(0,229,255,0.18); }

  .kp-entry-badge {
    display: inline-flex; align-items: center; padding: 5px 14px; border-radius: 999px;
    background: var(--primary); color: #fff; font-size: 12px; font-weight: 700;
    letter-spacing: 0.4px; text-transform: uppercase;
  }
  body.theme-dark .kp-entry-badge {
    background: transparent; border: 1px solid var(--accent); color: var(--accent);
    text-shadow: 0 0 8px var(--accent); box-shadow: 0 0 10px rgba(255,46,224,0.35);
    font-family: var(--font-display);
  }

  .kp-entry-remove {
    display: inline-flex; align-items: center; gap: 6px; padding: 6px 12px;
    border-radius: 8px; border: 1px solid var(--danger); background: transparent;
    color: var(--danger); font-size: 12.5px; font-weight: 600;
  }
  .kp-entry-remove:hover { background: var(--danger); color: #fff; }
  body.theme-dark .kp-entry-remove:hover { box-shadow: 0 0 12px var(--danger); }

  .kp-penilaian-block { margin-top: 8px; margin-bottom: 14px; }
  .kp-penilaian-header {
    display: flex; align-items: center; justify-content: space-between; margin-bottom: 10px;
  }
  .kp-small-btn { padding: 6px 12px; font-size: 12.5px; }

  .kp-panel .dyn-table select,
  .kp-panel .dyn-table input {
    padding: 8px 10px; font-size: 13px; border-radius: 7px;
    background: var(--kp-field-bg); color: var(--kp-field-text); border: 1px solid var(--kp-field-border);
  }
  body.theme-dark .kp-panel .dyn-table select,
  body.theme-dark .kp-panel .dyn-table input { font-family: 'JetBrains Mono', monospace; }
  .kp-panel .dyn-table th { background: var(--surface-alt); }
  body.theme-dark .kp-panel .dyn-table th {
    color: var(--primary); font-family: var(--font-display); letter-spacing: 0.5px;
  }

  @media (max-width: 720px) { .kp-panel .grid-2.kp-grid { grid-template-columns: 1fr; } }

  #view-kenaikan-pangkat .stack-field, #view-pensiun .stack-field { display: block; width: 100%; }
  #view-kenaikan-pangkat .stack-field > label, #view-pensiun .stack-field > label {
    display: block; width: 100%; margin-bottom: 7px;
  }
  #view-kenaikan-pangkat .stack-field input, #view-pensiun .stack-field input,
  #view-kenaikan-pangkat .stack-field select, #view-pensiun .stack-field select,
  #view-kenaikan-pangkat .field-input, #view-pensiun .field-input {
    display: block; width: 100%; box-sizing: border-box;
    padding: 14px 18px; min-height: 48px; border-radius: 11px;
    border: 1px solid var(--kp-field-border, var(--border));
    background: var(--kp-field-bg, var(--input-bg));
    color: var(--kp-field-text, var(--text));
    font-size: 14px;
  }
  .bsk-auto-field {
    background-color: #e2e8f0 !important;
    color: #0f172a !important;
    font-weight: 600 !important;
    border-color: #cbd5e1 !important;
  }
  body.theme-dark .bsk-auto-field {
    background-color: rgba(255, 255, 255, 0.08) !important;
    color: #f8fafc !important;
    border-color: rgba(255, 255, 255, 0.2) !important;
  }
  #view-kenaikan-pangkat .grid-2, #view-pensiun .grid-2 { grid-template-columns: 1fr 1fr; }
  #view-kenaikan-pangkat .kp-entry-badge, #view-pensiun .kp-entry-badge { display: inline-flex; }

  /* -------------------------------------------------------------------
     DASHBOARD ELIGIBLE & DASHBOARD PENSIUN
     ------------------------------------------------------------------- */
  .ce-dashboard {
    background: var(--surface-alt); border: 1px solid var(--border); border-radius: 14px;
    padding: 18px 20px;
  }
  body.theme-dark .ce-dashboard { border-color: rgba(0,229,255,0.2); }
  .ce-dashboard-header {
    display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 8px;
    margin-bottom: 14px;
  }
  .ce-dashboard-title { font-weight: 700; font-size: 14.5px; }
  body.theme-dark .ce-dashboard-title { font-family: var(--font-display); letter-spacing: 0.4px; }
  .ce-dashboard-target {
    font-size: 12px; font-weight: 600; padding: 4px 12px; border-radius: 999px;
    background: var(--primary); color: #fff;
  }
  body.theme-dark .ce-dashboard-target { background: transparent; border: 1px solid var(--accent); color: var(--accent); }

  .ce-dashboard-grid {
    display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 12px;
  }
  .ce-dashboard-card {
    cursor: pointer; text-align: left; padding: 14px 16px; border-radius: 11px;
    border: 1px solid var(--kp-card-border, var(--border)); background: var(--kp-card-bg, var(--surface));
    transition: transform var(--transition), box-shadow var(--transition), border-color var(--transition);
  }
  .ce-dashboard-card:hover { transform: translateY(-2px); box-shadow: var(--shadow); border-color: var(--primary); }
  body.theme-dark .ce-dashboard-card:hover { border-color: var(--accent); box-shadow: 0 0 14px rgba(255,46,224,0.25); }
  .ce-dashboard-card .cdc-title { font-weight: 700; font-size: 13.5px; margin-bottom: 8px; }
  .ce-dashboard-card .cdc-count { font-size: 22px; font-weight: 800; color: var(--primary); }
  body.theme-dark .ce-dashboard-card .cdc-count { color: var(--accent); text-shadow: 0 0 8px var(--accent); }
  .ce-dashboard-card .cdc-sub { font-size: 11.5px; color: var(--text-muted); margin-top: 2px; }
  .ce-dashboard-card.cdc-empty { opacity: 0.55; cursor: default; }
  .ce-dashboard-card.cdc-empty:hover { transform: none; box-shadow: none; border-color: var(--kp-card-border, var(--border)); }

  .ce-dashboard-back {
    background: none; border: none; color: var(--primary); font-weight: 600; font-size: 13px;
    cursor: pointer; padding: 6px 0; margin-bottom: 10px;
  }
  body.theme-dark .ce-dashboard-back { color: var(--accent); }

  .ce-dashboard-list { display: flex; flex-direction: column; gap: 8px; }
  .ce-dashboard-list-item {
    display: flex; align-items: center; justify-content: space-between; gap: 10px;
    padding: 11px 14px; border-radius: 999px; border: 1px solid var(--kp-card-border, var(--border));
    background: var(--kp-card-bg, var(--surface)); cursor: pointer;
    transition: border-color var(--transition), background var(--transition);
  }
  .ce-dashboard-list-item:hover { border-color: var(--primary); }
  body.theme-dark .ce-dashboard-list-item:hover { border-color: var(--accent); }
  .ce-dashboard-list-item .cdli-name { font-weight: 600; font-size: 13.5px; }
  .ce-dashboard-list-item .cdli-meta { font-size: 11.5px; color: var(--text-muted); }
  .ce-dashboard-list-item .cdli-badge {
    font-size: 10.5px; font-weight: 700; padding: 3px 9px; border-radius: 999px;
    background: var(--primary); color: #fff; white-space: nowrap;
  }
  body.theme-dark .ce-dashboard-list-item .cdli-badge { background: transparent; border: 1px solid var(--accent); color: var(--accent); }
  .cdli-warn {
    display: inline-block; font-size: 10.5px; font-weight: 700; padding: 2px 8px;
    border-radius: 999px; background: #fef3c7; color: #92400e; margin-left: 6px; vertical-align: middle;
  }
  body.theme-dark .cdli-warn { background: rgba(255, 200, 0, 0.12); color: #ffcc00; border: 1px solid rgba(255,200,0,0.4); }

  .ce-info-card {
    padding: 16px 18px; border-radius: 11px; border: 1px dashed var(--primary);
    background: var(--surface-alt); font-size: 13.5px; line-height: 1.6; margin-top: 14px;
  }
  body.theme-dark .ce-info-card { border-color: var(--accent); }

  /* -------------------------------------------------------------------
     PENSIUN
     ------------------------------------------------------------------- */
  .mk-input-pair { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .mk-display-row {
    display: flex; flex-wrap: wrap; gap: 14px; margin: 4px 0 18px;
    padding: 12px 16px; border-radius: 10px; background: var(--surface-alt);
    border: 1px solid var(--border); font-size: 12.5px;
  }
  body.theme-dark .mk-display-row { border-color: rgba(0,229,255,0.2); }
  .mk-display-row strong { color: var(--primary); }
  body.theme-dark .mk-display-row strong { color: var(--accent); }

  /* Neon animations */
  @keyframes neonFlicker {
    0%, 19%, 21%, 23%, 25%, 54%, 56%, 100% { opacity: 1; }
    20%, 24%, 55% { opacity: 0.7; }
  }
  body.theme-dark .neon-text {
    animation: neonFlicker 5s infinite;
  }

  /* ===================================================================
     RESPONSIVE ADAPTATION SYSTEM (Desktop & Mobile Modes)
     =================================================================== */

  /* Desktop Default Base (Width > 768px) */
  @media (min-width: 769px) {
    .mobile-only { display: none !important; }
    .desktop-only { display: flex !important; }
  }

  /* Mobile & Tablet Mode Adaptations (Width <= 768px) */
  @media (max-width: 768px) {
    .desktop-only { display: none !important; }
    .mobile-only { display: flex !important; }

    /* App Layout Reflow */
    .app-layout {
      flex-direction: column;
      min-height: 100vh;
    }

    /* Sidebar Drawer Transformation */
    .sidebar {
      width: 100% !important;
      max-width: 280px !important;
      position: fixed !important;
      top: 0 !important;
      left: -100% !important;
      height: 100vh !important;
      z-index: 9999 !important;
      transition: left 0.3s cubic-bezier(0.4, 0, 0.2, 1) !important;
      box-shadow: 4px 0 25px rgba(0, 0, 0, 0.5) !important;
    }
    .sidebar.mobile-open {
      left: 0 !important;
    }

    /* Main Content Container Adjustments */
    .main-content {
      padding: 14px 14px 80px 14px !important;
      margin-left: 0 !important;
      width: 100% !important;
      box-sizing: border-box !important;
    }

    /* Topbar Header Reflow */
    .topbar {
      padding-right: 0 !important;
      flex-wrap: wrap !important;
      gap: 10px !important;
    }
    .topbar h2 {
      font-size: 17px !important;
    }

    /* Responsive Grid & Bento Layout Reflow */
    div[style*="grid-template-columns"],
    .grid-2, .kp-grid, .grid-3 {
      grid-template-columns: 1fr !important;
    }
    div[style*="grid-column: span 8"],
    div[style*="grid-column: span 4"],
    div[style*="grid-column: span 6"] {
      grid-column: span 12 / span 12 !important;
    }

    /* Responsive Tables Wrapping */
    .table-wrapper, div:has(> table) {
      overflow-x: auto !important;
      -webkit-overflow-scrolling: touch !important;
      max-width: 100% !important;
    }
    table {
      min-width: 580px !important;
    }

    /* Tabs Bar Touch Scroll */
    .tabs {
      overflow-x: auto !important;
      -webkit-overflow-scrolling: touch !important;
      white-space: nowrap !important;
      padding-bottom: 6px !important;
    }
    .tab-btn {
      padding: 10px 14px !important;
      font-size: 12.5px !important;
    }

    /* Cards & Modals Padding */
    .card, .kp-panel, .mecha-border {
      padding: 16px !important;
    }

    /* Login Screen Card Responsive Shrink */
    .login-card {
      max-width: 94% !important;
      padding: 24px 18px !important;
    }
  }

  /* Ultra Mobile Small Screens (Width <= 480px) */
  @media (max-width: 480px) {
    .btn {
      padding: 9px 12px !important;
      font-size: 12.5px !important;
    }
    .ce-dashboard-header {
      flex-direction: column !important;
      align-items: flex-start !important;
    }
  }

  /* -------------------------------------------------------------------
     USULAN MASUK DASHBOARD & TMT GROUPING STYLES
     ------------------------------------------------------------------- */
  .um-dashboard-card {
    background: var(--surface-alt); border: 1px solid var(--border); border-radius: 14px;
    padding: 20px; box-shadow: var(--shadow);
  }
  body.theme-dark .um-dashboard-card { border-color: rgba(0,229,255,0.25); box-shadow: 0 0 16px rgba(0,229,255,0.1); }

  .um-dashboard-header {
    display: flex; align-items: center; justify-content: space-between; gap: 12px;
    margin-bottom: 16px; padding-bottom: 12px; border-bottom: 1px solid var(--border);
    flex-wrap: wrap;
  }

  .um-dashboard-body {
    display: grid; grid-template-columns: 240px 1fr; gap: 24px; align-items: center;
  }
  @media (max-width: 850px) {
    .um-dashboard-body { grid-template-columns: 1fr; }
  }

  .um-chart-container {
    display: flex; justify-content: center; align-items: center; position: relative;
    max-width: 240px; margin: 0 auto;
  }

  .um-stats-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(130px, 1fr)); gap: 12px;
  }

  .um-stat-card {
    padding: 14px 16px; border-radius: 12px; background: var(--surface);
    border: 2px solid var(--border); transition: transform 0.2s, box-shadow 0.2s;
    cursor: pointer;
  }
  .um-stat-card:hover { transform: translateY(-3px); box-shadow: 0 8px 20px rgba(0,0,0,0.12); }
  .um-stat-card .um-stat-value { font-size: 24px; font-weight: 800; margin-bottom: 4px; }
  .um-stat-card .um-stat-label { font-size: 12px; font-weight: 700; color: var(--text-muted); text-transform: uppercase; }

  .um-stat-card.stat-dsdm { border-color: #3b82f6; }
  .um-stat-card.stat-dsdm .um-stat-value { color: #2563eb; }
  .um-stat-card.stat-siasn { border-color: #f59e0b; }
  .um-stat-card.stat-siasn .um-stat-value { color: #d97706; }
  .um-stat-card.stat-pertek { border-color: #8b5cf6; }
  .um-stat-card.stat-pertek .um-stat-value { color: #7c3aed; }
  .um-stat-card.stat-sk { border-color: #10b981; }
  .um-stat-card.stat-sk .um-stat-value { color: #059669; }
  .um-stat-card.stat-perbaikan { border-color: #f97316; }
  .um-stat-card.stat-perbaikan .um-stat-value { color: #ea580c; }
  .um-stat-card.stat-belumbisa { border-color: #ef4444; }
  .um-stat-card.stat-belumbisa .um-stat-value { color: #dc2626; }
  .um-stat-card.stat-pensiun { border-color: #06b6d4; }
  .um-stat-card.stat-pensiun .um-stat-value { color: #0891b2; }
  .um-stat-card.stat-total { border-color: var(--primary); }
  .um-stat-card.stat-total .um-stat-value { color: var(--primary); }

  /* Status Badges */
  .badge-status {
    display: inline-flex; align-items: center; padding: 4px 12px; border-radius: 999px;
    font-size: 11.5px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.3px;
  }
  .badge-status.dsdm { background: #dbeafe; color: #1e40af; border: 1px solid #bfdbfe; }
  .badge-status.siasn { background: #fef3c7; color: #92400e; border: 1px solid #fde68a; }
  .badge-status.pertek { background: #f3e8ff; color: #6b21a8; border: 1px solid #e9d5ff; }
  .badge-status.sk { background: #d1fae5; color: #065f46; border: 1px solid #a7f3d0; }
  .badge-status.perbaikan { background: #ffedd5; color: #c2410c; border: 1px solid #fed7aa; }
  .badge-status.belumbisa { background: #fee2e2; color: #b91c1c; border: 1px solid #fca5a5; }
  .badge-status.pensiun { background: #cffafe; color: #0e7490; border: 1px solid #a5f3fc; }

  body.theme-dark .badge-status.dsdm { background: rgba(59,130,246,0.2); color: #60a5fa; border-color: #3b82f6; }
  body.theme-dark .badge-status.siasn { background: rgba(245,158,11,0.2); color: #fbbf24; border-color: #f59e0b; }
  body.theme-dark .badge-status.pertek { background: rgba(139,92,246,0.2); color: #c084fc; border-color: #8b5cf6; }
  body.theme-dark .badge-status.sk { background: rgba(16,185,129,0.2); color: #34d399; border-color: #10b981; }
  body.theme-dark .badge-status.perbaikan { background: rgba(249,115,22,0.2); color: #fb923c; border-color: #f97316; }
  body.theme-dark .badge-status.belumbisa { background: rgba(239,68,68,0.2); color: #f87171; border-color: #ef4444; }
  body.theme-dark .badge-status.pensiun { background: rgba(6,182,212,0.2); color: #22d3ee; border-color: #06b6d4; }

  /* TMT Section Grouping & Accordion */
  .tmt-year-card {
    margin-bottom: 20px; border-radius: 14px; border: 1px solid var(--border);
    background: var(--surface); overflow: hidden; box-shadow: var(--shadow);
  }
  .tmt-year-header {
    padding: 14px 20px; background: var(--surface-alt); border-bottom: 1px solid var(--border);
    display: flex; align-items: center; justify-content: space-between; font-weight: 700; font-size: 15px;
    cursor: pointer; transition: background 0.2s;
  }
  .tmt-year-header:hover { background: var(--primary-soft, rgba(0,55,176,0.06)); }
  .tmt-month-card {
    margin: 14px 16px 16px; padding: 16px; border-radius: 12px; border: 1px solid var(--border);
    background: var(--surface-alt);
  }
  .tmt-month-title {
    font-weight: 700; font-size: 14px; color: var(--primary); margin-bottom: 10px;
    display: flex; align-items: center; justify-content: space-between; cursor: pointer;
  }
  .tmt-table {
    width: 100%; border-collapse: collapse; font-size: 13px; margin-top: 8px;
  }
  .tmt-table th {
    background: var(--surface); padding: 10px 12px; text-align: left;
    border-bottom: 2px solid var(--border); font-weight: 700; color: var(--text-muted);
  }
  .tmt-table td {
    padding: 12px; border-bottom: 1px solid var(--border); vertical-align: middle;
  }
  .tmt-table tr:hover { background: rgba(0,0,0,0.02); }

  /* KONTRAK FORM DARK STYLING & HIGH CONTRAST TEXT VISIBILITY */
  .kontrak-select-dark {
    background-color: #1b365d !important;
    color: #ffffff !important;
    border: 1px solid #334e68 !important;
    border-radius: 6px !important;
    padding: 8px 12px !important;
    font-size: 13px !important;
    font-weight: 600 !important;
    outline: none !important;
    -webkit-appearance: menulist !important;
    appearance: menulist !important;
  }
  .kontrak-select-dark option {
    background-color: #1b365d !important;
    color: #ffffff !important;
  }

  .kontrak-form-wrap input.field-input-dark,
  .kontrak-form-wrap textarea.field-input-dark,
  .kontrak-form-wrap select.kontrak-select-dark,
  input.field-input-dark,
  textarea.field-input-dark {
    background-color: #1b365d !important;
    color: #ffffff !important;
    -webkit-text-fill-color: #ffffff !important;
    opacity: 1 !important;
    border: 1px solid #334e68 !important;
    border-radius: 8px !important;
    padding: 10px 14px !important;
    font-size: 14px !important;
    font-weight: 500 !important;
    width: 100% !important;
    box-sizing: border-box !important;
  }

  input.field-input-dark:disabled,
  input.field-input-dark:read-only,
  textarea.field-input-dark:disabled,
  textarea.field-input-dark:read-only,
  select.kontrak-select-dark:disabled {
    background-color: #162d4e !important;
    color: #ffffff !important;
    -webkit-text-fill-color: #ffffff !important;
    opacity: 1 !important;
    cursor: not-allowed !important;
    border-color: #274463 !important;
  }




</style>